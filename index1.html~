<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arun Periyal</title>
    <meta name="description" content="Arun Periyal's research in Naval Architecture, Fluid-Structure Interaction, Nonlinear Dynamics, and Machine Learning.">
    <meta name="keywords" content="Naval Architecture, Fluid Structure Interaction, CFD, FSI, Nonlinear Dynamics, VIV">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            color: #333;
            scroll-behavior: smooth;
        }

        /* Sidebar */
        .side-nav {
            width: 250px;
            background-color: #142f43;
            color: #fff;
            padding: 20px;
            height: 100vh;
            position: fixed;
            top: 0;
            left: -250px;
            transition: 0.3s;
            z-index: 1100; /* Higher than header */
        }
        .side-nav.active {
            left: 0;
        }
        .side-nav ul {
            list-style: none;
            padding: 0;
        }
        .side-nav ul li {
            margin-bottom: 15px;
        }
        .side-nav ul li a {
            color: #fff;
            text-decoration: none;
            font-size: 18px;
        }
        .menu-btn {
            position: fixed;
            top: 20px;
            left: 20px;
            background-color: #142f43;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            z-index: 1200;
        }

        /* Header */
        header {
    			position: relative;
    			background: url('https://raw.githubusercontent.com/arunperiyal/arunperiyal.github.io/main/figs/cover_image.jpg') no-repeat center center/cover;
    			color: #ffffff;
    			text-align: center;
    			padding: 8em 0 4em; /* Increased padding to prevent overlap */
    			margin-bottom: 0; /* No margin below */
    			z-index: 1100;
			}

        header::before {
			    content: '';
			    position: absolute;
			    top: 0;
			    left: 0;
			    width: 100%;
			    height: 100%;
			    background: rgba(0, 0, 0, 0.5);
			    z-index: 1100;
			}

        header h1, header p {
            position: relative;
            z-index: 2;
        }

        /* Navigation */
        nav {
    display: flex;
    justify-content: center;
    background-color: #142f43;
    padding: 1em 0;
    position: relative; /* Ensures it's separate from header */
    z-index: 1000; /* Keeps it above other elements */
}

        nav a {
    color: #ffffff;
    text-decoration: none;
    margin: 0 15px;
    font-weight: 500;
    padding: 10px;
    border-radius: 5px;
}

        nav a:hover, nav a.active {
    background-color: #f4d35e;
    color: #142f43;
}

        /* Carousel */
        .carousel {
            width: 100%;
            overflow: hidden;
            position: relative;
        }

        .carousel-track {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        .carousel-slide {
            min-width: 100%;
            background-color: #f8f8f8;
            text-align: center;
            padding: 2em;
        }

        .carousel-slide h3 {
            color: #142f43;
        }

        .carousel-controls {
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

        .carousel-controls button {
            padding: 10px;
            margin: 5px;
            background-color: #142f43;
            color: white;
            border: none;
            cursor: pointer;
        }

        /* Contact */
        .contact form label {
            font-weight: bold;
        }

        /* Newsletter */
        .newsletter {
            text-align: center;
            margin: 3em 0;
        }

        .newsletter input {
            padding: 10px;
            width: 250px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .newsletter button {
            padding: 10px;
            background-color: #142f43;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }

        /* Footer */
        footer {
            background-color: #142f43;
            color: #ffffff;
            padding: 2em 0;
            text-align: center;
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            // Sidebar Toggle
            const menuBtn = document.querySelector(".menu-btn");
            const sidebar = document.querySelector(".side-nav");
            menuBtn.addEventListener("click", () => {
                sidebar.classList.toggle("active");
            });

            // Carousel
            const track = document.querySelector('.carousel-track');
            const slides = document.querySelectorAll('.carousel-slide');
            let index = 0;

            function moveSlide(step) {
                index = (index + step + slides.length) % slides.length;
                track.style.transform = `translateX(-${index * 100}%)`;
            }

            document.getElementById("prev").addEventListener("click", () => moveSlide(-1));
            document.getElementById("next").addEventListener("click", () => moveSlide(1));

            setInterval(() => moveSlide(1), 3000);
        });
    </script>
</head>
<body>
    <button class="menu-btn">&#9776;</button>
    <aside class="side-nav">
        <h2>Navigation</h2>
        <ul>
            <li><a href="index.html" class="active">Home</a></li>
            <li><a href="useful_links.html">Useful links</a></li>
            <li><a href="latest_scripts.html">Latest Scripts</a></li>
            <li><a href="curriculum_vitae.html">Curriculum Vitae</a></li>
        </ul>
    </aside>
    <header>
        <h1>Arun Periyal</h1>
    </header>
    <nav>
        <a href="research.html">Research</a>
        <a href="materials.html">Materials</a>
        <a href="about.html">About Me</a>
    </nav>
    <section class="carousel">
        <div class="carousel-track">
            <div class="carousel-slide">
                <h3>Energy Harvesting Devices</h3>
                <p>Parametric Study and Stability Analysis.</p>
            </div>
            <div class="carousel-slide">
                <h3>Marine riser VIV suppression Using Grooves</h3>
                <p>Efficient grooving methods based on structure responses.</p>
            </div>
        </div>
        <div class="carousel-controls">
            <button id="prev">&laquo; Prev</button>
            <button id="next">Next &raquo;</button>
        </div>
    </section>
    <section class="newsletter">
        <h2>Subscribe for Updates</h2>
        <input type="email" placeholder="Enter your email">
        <button>Subscribe</button>
    </section>
    <footer>
        <p>&copy; 2024 Arun Periyal. All rights reserved.</p>
    </footer>
</body>
</html>
